<?php
    // echo readfile("file.txt");


    // $myfile = fopen("file.txt","r") or  die("lỗi"); // mở file
    // echo fread($myfile, filesize("file.txt"));  // đọc file nhưng đuôi k đếm kí tự

    // lặp đọc file
    // while(!feof($myfile)){
    //     echo  fgets($myfile);
    // }
    // fclose($myfile); // đóng file


    //tạo 1 file mới = code

    // $myfile=fopen ("newfile.txt","w"); w quyền tao file

    // viết vào file và đè lên file
    // $myfile=fopen ("newfile.txt","a") or die("lỗi"); // a quyền viết thêm vào file
        // fwrite($myfile,"xin chao luan");
        // fclose($myfile);


        
?>